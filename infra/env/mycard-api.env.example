# =====================================================
# MyCard API Environment Configuration
# Location: /etc/mycard/mycard-api.env
#
# ⚠️ SECURITY WARNING
# - Never commit actual credentials to version control
# - Set restrictive permissions: chmod 600 /etc/mycard/mycard-api.env
# - This file should only be readable by root and mycard user
# =====================================================

# --------------------- Spring Profile ---------------------
SPRING_PROFILES_ACTIVE=prod

# --------------------- Server Settings ---------------------
SERVER_PORT=8080

# --------------------- Database (MySQL) ---------------------
DB_URL=jdbc:mysql://localhost:3306/mycard?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
DB_USERNAME=mycard_app
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# --------------------- JWT Settings ---------------------
# Generate secure secret: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_64
JWT_ACCESS_TOKEN_VALIDITY_MS=900000
JWT_REFRESH_TOKEN_VALIDITY_MS=604800000
JWT_ABSOLUTE_SESSION_VALIDITY_MS=2592000000
# 관리자 비밀번호 변경 시 검증할 RSA 공개키 PEM (한 줄 입력 시 \n 사용)
ADMIN_PASSWORD_CHANGE_PUBLIC_KEY_PEM=-----BEGIN PUBLIC KEY-----\nREPLACE_WITH_RSA_PUBLIC_KEY\n-----END PUBLIC KEY-----
# --------------------- File Upload ---------------------
UPLOAD_PATH=/var/lib/mycard/uploads

# --------------------- CORS Settings ---------------------
# 프론트엔드 서버 IP/도메인 (쉼표로 구분)
# 예: http://192.168.10.137,http://192.168.10.137:8081
CORS_ALLOWED_ORIGINS=http://YOUR_FRONTEND_IP,http://YOUR_FRONTEND_IP:8081

# --------------------- Logging ---------------------
LOGGING_LEVEL_ROOT=WARN
LOGGING_LEVEL_COM_MYCARD=INFO
LOG_PATH=/var/log/mycard

# --------------------- Flyway ---------------------
SPRING_FLYWAY_ENABLED=true
SPRING_FLYWAY_BASELINE_ON_MIGRATE=true
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
